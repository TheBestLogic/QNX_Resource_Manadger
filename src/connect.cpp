#include "connect.h"
#include "program.h"
using namespace std;
using namespace DataSend;

void DoConnect::StartTimer( Data &D )
	{
		//create attrib if need
		while( 1 )
		{
			/*pthread_create(NULL, &attr, new_thread, NULL);
			pthread_create(NULL, &attr, new_thread, NULL);
			int pthread_join(pthread_t thread, void **value_ptr);
			int pthread_join(pthread_t thread, void **value_ptr);
			sleep(20);*/
		}
	}
